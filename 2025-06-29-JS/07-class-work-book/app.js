const book = {
    price: 100,
    Author: 'Israel Israel',
    Publisher: 'tel-aviv drop book',
    Pages: 156,
}
book.price = book.price * 0.8
document.write(` A new price for book  ${book.price}`)
